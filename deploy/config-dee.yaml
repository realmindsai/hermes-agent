# Hermes Agent - Dee's Configuration
# Secrets (API keys, tokens) live in .env via SOPS, not here.

model:
  default: "gpt-5.4"
  provider: "openai-codex"

terminal:
  backend: "local"
  cwd: "/workspace"
  timeout: 180
  lifetime_seconds: 300

memory:
  mode: "local"

# MCP servers - shared tooling
# mcp_servers:
#   filesystem:
#     command: "npx"
#     args: ["@modelcontextprotocol/server-filesystem", "/tmp"]
#   jot:
#     command: "npx"
#     args: ["@anthropic-ai/jot-mcp"]
#     env:
#       JOT_TOKEN: "${JOT_TOKEN}"
